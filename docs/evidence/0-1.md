# Evidence record: slice 0.1

Consolidate the fixture tree; add missing-field, unknown-field, max-size, and old-agent cases.

- **slice**: 0.1 (`Consolidate the fixture tree; add missing-field, unknown-field, max-size, and old-agent cases`)
- **prerequisite**: R0 (accepted by operator on 2026-09-03)
- **owned files**: `hub/tests/fixtures/response/`, `hub/pkg/response/fixtures_test.go`, `hub/pkg/response/dto.go`, `agent/tests/test_response_fixtures.c`
- **protocol/schema version**: Response wire protocol version 1 (`GrantVersion = 1`).
- **runtime target**: Cross-language test fixtures shared between Hub (Go) and Agents (C).

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet), consistently throughout.
> Digests, timestamps, sizes, exit codes and every other value are verbatim.

## Status claimed

`verified`. The fixture tree is consolidated into a single canonical directory (`hub/tests/fixtures/response`), the duplicate tree `tests/fixtures/` was deleted, 21 comprehensive cross-language fixtures were generated, and both Go and C test suites validate them cleanly.

## Claims

1. Consolidated the fixture tree into `hub/tests/fixtures/response` and eliminated the duplicate `tests/fixtures/response` directory.
2. Implemented `Validate()` methods on `EndpointGrant`, `JobOffer`, `JobAck`, and `JobResult` enforcing protocol versioning, identifier requirements, action kind constraints, digest bounds, and signature encodings.
3. Generated and tested 21 comprehensive cross-language fixtures:
   - **Baseline Valid:** `grant_valid.json`, `offer_valid.json`, `ack_valid.json`, `result_valid.json`
   - **Missing Optional Fields:** `offer_missing_optional.json`, `ack_missing_optional.json`, `result_missing_optional.json`
   - **Missing Required Fields (Negative):** `grant_missing_required.json`, `offer_missing_required.json`, `ack_missing_required.json`, `result_missing_required.json`
   - **Unknown Extra Fields (Forward-Compatibility):** `grant_unknown_fields.json`, `offer_unknown_fields.json`, `ack_unknown_fields.json`, `result_unknown_fields.json`
   - **Boundary / Max-Size:** `grant_max_size.json`, `offer_max_size.json`
   - **Old-Agent Compatibility:** `heartbeat_response_old_agent.json` (proves legacy agents safely ignore `response_offers` while processing `quarantined_peers` and `agent_update`)
   - **Cryptographic Failure / Tampering:** `grant_expired.json`, `grant_tampered.json`, `grant_wrong_key.json`
4. Created C agent test `agent/tests/test_response_fixtures.c` consuming and validating fixtures in C, confirming field extraction, required field checking, unknown field tolerance, and legacy telemetry parsing.
5. All tests in `hub/pkg/response` pass with the race detector enabled.

## Verification Commands & Output

### 1. Go Fixture Generation & Validation Test
```console
$ cd hub && go test -v -run TestGenerateAndValidateCrossLanguageFixtures ./pkg/response
=== RUN   TestGenerateAndValidateCrossLanguageFixtures
--- PASS: TestGenerateAndValidateCrossLanguageFixtures (0.00s)
PASS
ok  	ominull/hub/pkg/response	0.005s
```

### 2. C Cross-Language Fixture Test
```console
$ gcc -Wall -Wextra -Wformat=2 -O2 -o /tmp/test_response_fixtures agent/tests/test_response_fixtures.c && /tmp/test_response_fixtures hub/tests/fixtures/response
  [+] heartbeat status extracted
  [+] heartbeat contains quarantined_peers
  [+] heartbeat contains agent_update
  [+] heartbeat contains response_offers
  [+] grant_valid has valid grant_id
  [+] grant_valid has 64-char action_digest
  [+] grant_valid has 128-char hex signature
  [+] grant_missing_required lacks endpoint_id
  [+] grant_missing_required lacks signature
  [+] offer_unknown_fields has valid job_id
  [+] offer_unknown_fields has valid lease_id
  [+] offer_unknown_fields carries unknown field priority
  [+] offer_max_size is non-empty bounded payload
[+] All C agent fixture validation checks passed.
```

### 3. Verification of Zero Duplicate Fixtures
```console
$ ls -d tests/fixtures 2>/dev/null || echo "No duplicate tests/fixtures directory"
No duplicate tests/fixtures directory
```
