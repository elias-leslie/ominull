# Evidence record: slice 0.2

Shared length-prefixed canonical encoder with byte-exact Go and C fixtures.

- **slice**: 0.2 (`Shared length-prefixed canonical encoder with byte-exact Go and C fixtures`)
- **prerequisite**: 0.1 (`5003994: feat(response): slice 0.1 - consolidate fixture tree and add edge/compat fixtures`)
- **owned files**: `hub/pkg/response/canonical.go`, `hub/pkg/response/dto.go`, `hub/pkg/responseauth/dto.go`, `hub/pkg/responseauth/authority.go`, `agent/include/response_canonical.h`, `agent/tests/test_response_canonical.c`, `hub/tests/fixtures/response/grant_v2_canonical.bin`, `hub/tests/fixtures/response/grant_v2_canonical.hex`
- **protocol/schema version**: Response grant and proof version 2 (`GrantVersion = 2`, `ProofVersion = 2`). Length-prefixed canonical binary encoding.
- **runtime target**: Hub (Go) and Agents (Linux/Windows C).

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet), consistently throughout.
> Digests, timestamps, sizes, exit codes and every other value are verbatim.

## Status claimed

`verified`. The fragile colon-separated `fmt.Sprintf` canonicalization has been completely replaced with a deterministic length-prefixed binary encoder implemented symmetrically in Go and C. Byte-exact cross-language identity is verified against binary fixtures.

## Claims

1. Replaced colon-joined `fmt.Sprintf` string joining with length-prefixed binary encoding for `EndpointGrant` (`CanonicalBytes()`) and `ActionProof` (`CanonicalBytes()`).
2. Eliminated delimiter injection vulnerabilities: all variable-length fields (strings and byte slices) carry a 4-byte big-endian length prefix, fixed-width integers are encoded big-endian (4 bytes for uint32, 8 bytes for int64), and domain-separation labels are prepended.
3. Bumped protocol versions to `GrantVersion = 2` and `ProofVersion = 2` per plan specifications.
4. Generated byte-exact cross-language fixtures (`grant_v2_canonical.bin` and `grant_v2_canonical.hex`).
5. Created C test `agent/tests/test_response_canonical.c` which parses the Go-generated binary fixture and asserts byte-for-byte, bit-for-bit equality with C-encoded canonical bytes.
6. Updated `hub/pkg/responseauth/authority.go` to sign `grant.CanonicalBytes()`.

## Verification Commands & Output

### 1. Byte-Exact Cross-Language Test in C
```console
$ gcc -Wall -Wextra -Wformat=2 -O2 -Iagent/include -o /tmp/test_response_canonical agent/tests/test_response_canonical.c && /tmp/test_response_canonical hub/tests/fixtures/response/grant_v2_canonical.bin
  [+] canonical grant encoding succeeded
  [+] canonical grant length matches fixture exactly
  [+] canonical grant bytes match Go-generated fixture byte-for-byte
  [+] canonical proof encoding succeeded
[+] All response canonical encoder tests passed byte-exact verification.
```

### 2. Go Response and ResponseAuth Race Tests
```console
$ (cd hub && go test -v -race ./pkg/response ./pkg/responseauth)
=== RUN   TestEndpointGrant_Verification
--- PASS: TestEndpointGrant_Verification (0.00s)
=== RUN   TestComputeActionDigest
--- PASS: TestComputeActionDigest (0.00s)
=== RUN   TestGenerateAndValidateCrossLanguageFixtures
--- PASS: TestGenerateAndValidateCrossLanguageFixtures (0.01s)
=== RUN   TestStore_JobLifecycle
--- PASS: TestStore_JobLifecycle (0.02s)
PASS
ok  	ominull/hub/pkg/response	1.039s
=== RUN   TestAuthority_FullLifecycle
--- PASS: TestAuthority_FullLifecycle (0.00s)
=== RUN   TestActionProof_Verification
--- PASS: TestActionProof_Verification (0.00s)
=== RUN   TestResponseSession_Validation
--- PASS: TestResponseSession_Validation (0.00s)
=== RUN   TestUDSClientAndServer
--- PASS: TestUDSClientAndServer (0.06s)
=== RUN   TestTOTP_GenerationAndVerification
--- PASS: TestTOTP_GenerationAndVerification (0.00s)
PASS
ok  	ominull/hub/pkg/responseauth	1.072s
```
