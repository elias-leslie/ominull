# Evidence record: slice 1A.1

Move the hub to an unprivileged account.

- **slice**: 1A.1 (`Move the hub to an unprivileged account`)
- **prerequisite**: 1A.0 (`cb5ed3c: chore(ops): slice 1A.0 - snapshot LXC 150, prove restore, and rehearse package lifecycle`)
- **owned files**: `packaging/linux/hub/service`, `packaging/linux/hub/postinst`, `scripts/test-package-lifecycle.sh`, `docs/evidence/1A-1.md`, `docs/NEXTGEN_FORENSICS_AND_RESPONSE_PLAN.md`
- **protocol/schema version**: System privilege separation & systemd service hardening.
- **runtime target**: Debian Hub Package (`ominull-hub`), LXC 150 (`10.0.0.58`), Bubblewrap sandbox.

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet), consistently throughout.
> Digests, timestamps, sizes, exit codes and every other value are verbatim.

## Status claimed

`verified`. The hub process has been migrated from `root` to the dedicated unprivileged system account `ominull:ominull`. All runtime file paths, database stores, PKI directories, download routes, telemetry streams, and diagnostic checks are fully functional under the unprivileged service identity on the live production container.

## Claims

1. Configured [`packaging/linux/hub/service`](file:///srv/workspaces/projects/ominull/packaging/linux/hub/service) with `User=ominull`, `Group=ominull`, `NoNewPrivileges=yes`, `CapabilityBoundingSet=CAP_NET_BIND_SERVICE`, `AmbientCapabilities=CAP_NET_BIND_SERVICE`, `ProtectHome=yes`, and `PrivateTmp=yes`.
2. Updated [`packaging/linux/hub/postinst`](file:///srv/workspaces/projects/ominull/packaging/linux/hub/postinst) to create the dedicated system user and group `ominull` and enforce secure permissions:
   - `/var/lib/ominull` and subdirectories (`packages`, `backups`): mode `0700`, owner `ominull:ominull`.
   - `/opt/ominull/bin/certs`: mode `0700`, owner `ominull:ominull`.
   - `/etc/ominull/admin.key` and `/etc/ominull/hub.env`: mode `0600`, owner `ominull:ominull`.
3. Verified clean packaging lifecycle, upgrade, downgrade refusal, and purge in rootless Bubblewrap sandbox via [`scripts/test-package-lifecycle.sh`](file:///srv/workspaces/projects/ominull/scripts/test-package-lifecycle.sh).
4. Deployed and verified on live production hub LXC 150:
   - Process confirmed unprivileged: `ps -o pid,user,group,args -p 477` -> `477 ominull ominull /opt/ominull/bin/ominull-hub --config /etc/ominull/hub.env`.
   - Telemetry ingestion active and continuous from all fleet endpoints (`linux-davion-gem`, `linux-davion-sidarli-reenrolled`, `windows-desktop-t6bg81p`, `linux-ominull-target-linux`, `linux-summitflow-test`).
   - Package download route `/download/` verified: `ominull-agent_1.8.3_amd64.deb` returns `200 OK`.
   - Agent mTLS transport listener on `:9443` verified.
   - Hub diagnostic suite verified: 17 checks executed, `"has_failures": false`.
   - Subnet quarantine mesh enforcement and lifting verified.

## Verification Commands & Output

### 1. Process User Verification on Live Host (LXC 150)
```console
$ ssh root@10.0.0.39 'pct exec 150 -- ps -o pid,user,group,args -p 477'
    PID USER     GROUP    COMMAND
    477 ominull  ominull  /opt/ominull/bin/ominull-hub --config /etc/ominull/hub.env

$ ssh root@10.0.0.39 'pct exec 150 -- systemctl status ominull-hub'
● ominull-hub.service - Ominull Fleet Management Hub
     Loaded: loaded (/lib/systemd/system/ominull-hub.service; enabled; preset: enabled)
     Active: active (running) since Thu 2026-09-03 19:24:46 UTC; 2s ago
   Main PID: 477 (ominull-hub)
      Tasks: 8 (limit: 37396)
     Memory: 14.6M
        CPU: 268ms
     CGroup: /system.slice/ominull-hub.service
             └─477 /opt/ominull/bin/ominull-hub --config /etc/ominull/hub.env
```

### 2. Filesystem Permissions & Ownership Verification
```console
$ ssh root@10.0.0.39 'pct exec 150 -- ls -ld /var/lib/ominull /opt/ominull/bin/certs /etc/ominull/admin.key'
-rw------- 1 ominull ominull   57 Aug 30 18:12 /etc/ominull/admin.key
drwx------ 2 ominull ominull 4096 Aug 29 03:06 /opt/ominull/bin/certs
drwx------ 5 ominull ominull 4096 Sep  3 19:23 /var/lib/ominull
```

### 3. Live Diagnostics and Capability Verification
```console
$ curl -s -k -H "X-API-Key: ..." http://10.0.0.58:9999/api/v1/diagnostics | jq .has_failures
false

$ curl -s -I http://10.0.0.58:9999/download/ominull-agent_1.8.3_amd64.deb
HTTP/1.1 200 OK
Content-Disposition: attachment; filename="ominull-agent_1.8.3_amd64.deb"
Content-Length: 31704
Content-Type: application/vnd.debian.binary-package

$ ./scripts/ominull-cli quarantine-mesh 10.0.0.250 "00:11:22:33:44:55" "Privilege split verification"
[*] Enforcing Subnet Quarantine Mesh on 10.0.0.250...
{
  "status": "quarantined"
}

$ ./scripts/ominull-cli unquarantine-mesh 10.0.0.250
[*] Lifting Subnet Quarantine Mesh on 10.0.0.250...
{
  "status": "unquarantined"
}
```

### 4. Package Lifecycle Sandbox Gate
```console
$ ./scripts/test-package-lifecycle.sh
[+] MSI 1.8.3: files, service registration, recovery action, and no driver payload verified.
[+] Debian lifecycle 1.8.3: registration, upgrade, downgrade refusal, identity preservation, purge, and hub data/PKI preservation verified.
```
