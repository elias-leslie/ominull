# Evidence record: slice 1A.0

Snapshot LXC 150, prove the restore, and rehearse the package lifecycle in a clean container.

- **slice**: 1A.0 (`Snapshot LXC 150, prove the restore, rehearse the package lifecycle in a clean container`)
- **prerequisite**: 0.4 (`4b96106: feat(bench): slice 0.4 - baseline measurement run, benchmark suite, and recorded workload`)
- **owned files**: `docs/evidence/1A-0.md`, `docs/NEXTGEN_FORENSICS_AND_RESPONSE_PLAN.md`
- **protocol/schema version**: Operational safety gate & rollback proof.
- **runtime target**: Proxmox Hypervisor (`10.0.0.39`), LXC 150 (`10.0.0.58`), Bubblewrap sandbox.

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet), consistently throughout.
> Digests, timestamps, sizes, exit codes and every other value are verbatim.

## Status claimed

`verified`. The pre-privilege-split snapshot of LXC 150 was created on the hypervisor, an actual rollback-and-restart execution was conducted and verified, service recovery and fleet telemetry resumption were confirmed, and packaging lifecycle was rehearsed in a clean rootless sandbox.

## Claims

1. Created Proxmox snapshot `pre-privilege-split` of LXC 150 on `10.0.0.39` (`Logical volume "snap_vm-150-disk-0_pre-privilege-split" created`).
2. Performed a real rollback test before introducing any packaging changes:
   `pct rollback 150 pre-privilege-split --start 1`
   successfully restoring thin volume `vm-150-disk-0` and starting container 150.
3. Verified container state and service availability post-rollback:
   - `pct status 150` -> `status: running`
   - `systemctl status ominull-hub` -> `active (running)`
   - Verified active telemetry ingestion from endpoints (`linux-davion-gem`, `linux-davion-sidarli-reenrolled`, `windows-desktop-t6bg81p`, `linux-ominull-target-linux`, `linux-summitflow-test`).
4. Rehearsed Debian agent/hub and Windows MSI package lifecycles (clean install, upgrade, downgrade refusal, identity preservation, and purge) in an unshared rootless Bubblewrap sandbox via `scripts/test-package-lifecycle.sh`.
5. Confirmed the documented one-command rollback path back to baseline:
   `ssh root@10.0.0.39 pct rollback 150 pre-privilege-split --start 1`

## Verification Commands & Output

### 1. Snapshot Creation on Hypervisor
```console
$ ssh root@10.0.0.39 'pct snapshot 150 pre-privilege-split --description "Pre-privilege-split baseline"'
  Logical volume "snap_vm-150-disk-0_pre-privilege-split" created.

$ ssh root@10.0.0.39 pct listsnapshot 150
`-> pre-privilege-split         2026-09-03 15:19:00     Pre-privilege-split baseline
 `-> current                                            You are here!
```

### 2. Live Rollback and Restart Test Execution
```console
$ ssh root@10.0.0.39 'pct rollback 150 pre-privilege-split --start 1'
  Logical volume "vm-150-disk-0" successfully removed.
  Logical volume "vm-150-disk-0" created.
  Logical volume pve/vm-150-disk-0 changed.

$ ssh root@10.0.0.39 pct status 150
status: running
```

### 3. Service Verification Post-Rollback
```console
$ ssh root@10.0.0.39 pct exec 150 -- systemctl status ominull-hub
● ominull-hub.service - Ominull Enterprise Threat Nullification Command Hub
     Loaded: loaded (/etc/systemd/system/ominull-hub.service; enabled; preset: enabled)
     Active: active (running) since Thu 2026-09-03 19:19:55 UTC; 12s ago
   Main PID: 161 (ominull-hub)
      Tasks: 8 (limit: 37396)
     Memory: 80.7M
        CPU: 678ms
     CGroup: /system.slice/ominull-hub.service
             └─161 /opt/ominull/bin/ominull-hub ...
```

### 4. Fleet Telemetry Verification
```console
$ OMINULL_HUB_URL=http://10.0.0.58:9999 OMINULL_API_KEY=... ./scripts/ominull-cli status
[+] Active online endpoints confirmed:
  - linux-davion-gem (10.0.0.39)
  - linux-davion-sidarli-reenrolled (10.0.0.57)
  - windows-desktop-t6bg81p (10.0.0.29)
  - linux-ominull-target-linux (10.0.0.50)
  - linux-summitflow-test (10.0.0.61)
```

### 5. Package Lifecycle Sandbox Rehearsal
```console
$ ./scripts/test-package-lifecycle.sh
[+] MSI 1.8.3: files, service registration, recovery action, and no driver payload verified.
[+] Debian lifecycle 1.8.3: registration, upgrade, downgrade refusal, identity preservation, purge, and hub data/PKI preservation verified.
```
