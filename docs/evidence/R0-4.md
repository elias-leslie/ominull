# Evidence record: slice R0.4

Remove the console shell modal and its entry points.

- **slice**: R0.4 (`Remove the console shell modal and its entry points`)
- **prerequisite commit**: `a7a4fb5` (`R0.3: fix ominullctl build, remove shell open/exec, token printing, and plaintext key fallback`)
- **owned files**: `hub/pkg/server/web/app.js`, `hub/pkg/server/console_cache_test.go`
- **protocol/schema version**: unchanged. No database migration, agent protocol change, or package schema change.
- **runtime target**: Hub web console single-page application (`/app.js`, `/`).
- **release version**: none published by this slice.

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet), consistently throughout.
> Digests, timestamps, sizes, exit codes and every other value are verbatim.

## Status claimed

`verified`. The console contains no control that claims a capability listed as absent in the audit table, syntax check passes, and test asserts absence of all forbidden claims.

## Claims

1. Removed the "Open remote shell…" action menu item from the managed asset context menu in `hub/pkg/server/web/app.js`.
2. Removed the terminal modal implementation and session management routines (`openTerminalSession`, `showTerminalModal`, `closeTerminalModal`, `terminalModalEl`) that claimed nonexistent capabilities:
   - "SESSION ACTIVE"
   - "Connected to <endpoint> ... via Ominull Relay"
   - "Interactive pseudoterminal ready"
   - "Frames encrypted & audited"
   - "[Command queued on endpoint heartbeat relay]"
3. Removed the "Interactive Remote Shell (ConPTY / forkpty)" card and its "Launch Shell" form control from the Response view (`renderResponse`).
4. Added server test `TestConsoleContainsNoUnimplementedShellControls` asserting that `/app.js` as served by the hub contains none of the forbidden strings or unimplemented shell controls.
5. JavaScript passes `node --check`.

## Verification Commands & Output

### 1. JavaScript Syntax Check
```console
$ node --check hub/pkg/server/web/app.js
```
Exited with 0 errors.

### 2. Automated Test in Hub Server Package
```console
$ cd hub && go test -v -run TestConsoleContainsNoUnimplementedShellControls ./pkg/server
=== RUN   TestConsoleContainsNoUnimplementedShellControls
--- PASS: TestConsoleContainsNoUnimplementedShellControls (0.24s)
PASS
ok  	ominull/hub/pkg/server	0.239s
```

### 3. Verification of Zero Forbidden String Occurrences in Assets
```console
$ grep -E "openTerminalSession|terminal-modal|SESSION ACTIVE|Open remote shell|Interactive Remote Shell|via Ominull Relay" hub/pkg/server/web/app.js
```
Returned zero matches.
