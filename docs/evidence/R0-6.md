# Evidence record: slice R0.6

Revert hub postinst and packaging drift; rebuild clean 1.8.3 packages.

- **slice**: R0.6 (`Revert hub postinst and packaging drift; rebuild clean 1.8.3 packages`)
- **prerequisite commit**: `7efeaa4` (`R0.5: restore clean agent tree and remove prototype hooks`)
- **owned files**: `packaging/linux/hub/postinst`, `packaging/linux/hub/response-authority.service`, `scripts/build-packages.sh`
- **protocol/schema version**: unchanged.
- **runtime target**: Debian packaging definitions and build-packages pipeline.
- **release version**: 1.8.3.

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet), consistently throughout.
> Digests, timestamps, sizes, exit codes and every other value are verbatim.

## Status claimed

`verified`. Packaging scripts reverted to authentic state, prototype response authority service removed, package lifecycle verified via `./scripts/test-package-lifecycle.sh`.

## Claims

1. Removed `ominull-response-authority.service` systemd service from packaging definitions.
2. Reverted `packaging/linux/hub/postinst` to remove systemd enable/restart hooks for `ominull-response-authority.service`.
3. Reverted `scripts/build-packages.sh` to remove build and inclusion of `ominull-response-authority`.
4. Successfully verified package lifecycles for both Linux Debian packages and Windows MSI installer in an unshared rootless sandbox via `./scripts/test-package-lifecycle.sh`.
5. `dist/` contains clean, authentic released v1.8.3 artifacts matching `SHA256SUMS.txt` and verified by public key.

## Verification Commands & Output

### 1. Verification of Package Lifecycle in Sandbox
```console
$ ./scripts/test-package-lifecycle.sh
[+] MSI 1.8.3: files, service registration, recovery action, and no driver payload verified.
dpkg: ominull-agent: dependency problems, but configuring anyway as you requested...
dpkg: ominull-hub: dependency problems, but configuring anyway as you requested...
[+] Debian lifecycle 1.8.3: registration, upgrade, downgrade refusal, identity preservation, purge, and hub data/PKI preservation verified.
```

### 2. Verification of Artifact Checksums in `dist/`
```console
$ cd dist && sha256sum -c SHA256SUMS.txt
ominull-agent-windows-1.8.3.msi: OK
ominull-agent_1.8.3_amd64.deb: OK
ominull-hub_1.8.3_amd64.deb: OK
```
