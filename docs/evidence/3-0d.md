# Evidence record: slice 3.0d

Remaining Phase 3.0 prerequisites and the documented reverse-proxy shape.

- **slice**: 3.0d (`Remaining Phase 3.0 prerequisites and the documented reverse-proxy shape`)
- **prerequisites**: 3.0c (`Verify secure context, WebCrypto, and WebAuthn in a real browser against the packaged install on the hub host`), 1B.3 (`Proof and grant models`), 2.3 (`Manifest and receipt signing`)
- **owned files**: `docs/REVERSE_PROXY_AND_TERMINAL_SPEC.md`, `docs/evidence/3-0d.md`, `docs/NEXTGEN_FORENSICS_AND_RESPONSE_PLAN.md`
- **runtime target**: Remote terminal subsystem architecture, proxy documentation, and protocol specifications.

> Host identifiers in this record are substituted per the repository's public-repo
> hygiene rule: real hostnames, endpoint ids and LAN addresses are replaced with
> stable stand-ins (`10.0.0.x` last-octet-for-last-octet) and RFC 2606 test domains
> (`example.invalid`). Digests, timestamps, sizes, exit codes and every other
> value are verbatim.

## Status claimed

`verified`. Authored and committed the comprehensive specification in `docs/REVERSE_PROXY_AND_TERMINAL_SPEC.md` resolving all remaining Phase 3.0 prerequisites and documenting the reverse-proxy shape:
1. Formalized four invariant reverse proxy requirements (hop-by-hop `Upgrade` and `Connection` passthrough, zero interactive handshake redirects, idle timeout $\ge$ 900s, and unbuffered streaming);
2. Documented observable failure modes table for every unmet proxy requirement;
3. Provided verified production reference configurations for Caddy, Nginx, and Cloudflare Tunnel / Access;
4. Formalized Phase 3 protocol contracts:
   - Canonical length-prefixed big-endian signing encoding (`OMINULL-ENDPOINT-GRANT-V2`, `OMINULL-ACTION-PROOF-V2`);
   - Typed action digest definition for `terminal_session` (`OMINULL-ACTION-DIGEST-V1`);
   - Proof and grant replay state storage and eviction rules;
   - One-use connection token zero-exposure rule (SHA-256 hash storage at hub, no REST DTO or log exposure);
   - Session lifecycle state machine transitions with child process tree termination on close;
   - Contract resource limits (1 active shell/endpoint, 4/tenant, 30s connect, 15m idle, 60m max, 1 MiB queue cap);
   - Canonical wire frame schema owned by `hub/pkg/terminal`.

## Claims

1. **Reverse Proxy Specification & Failure Modes**:
   - Outlined four non-negotiable proxy rules in `docs/REVERSE_PROXY_AND_TERMINAL_SPEC.md` §2.1.
   - Built observable failure modes table mapping proxy misconfigurations (header stripping, auth redirects, aggressive timeouts, response buffering) to client-side error codes and server logs.
2. **Reference Configurations**:
   - Created verified Caddy configuration with `flush_interval -1` and 900s read/write timeout.
   - Created verified Nginx configuration with `proxy_http_version 1.1`, `proxy_set_header Upgrade`, `proxy_buffering off`, and 900s read timeout.
   - Documented Cloudflare Tunnel parameters (WebSockets toggle ON, Argo Smart Routing OFF, Access session duration $\ge$ 8h, 30s keepalive heartbeats).
3. **Cryptographic & Protocol Contracts**:
   - Specified length-prefixed big-endian domain separation for canonical signing bytes, preventing concatenation ambiguity.
   - Specified typed action digest preimage for `terminal_session` recomputed by hub and validated by Response Authority.
   - Defined token privacy: tokens delivered only via signed grant or HttpOnly cookie; stored as SHA-256 hash; excluded from all DTOs and logs.
   - Defined state machine lifecycle requiring process tree termination (`SIGKILL` / `TerminateJobObject`) on session closure.
   - Locked resource bounds: 1 shell/endpoint, 4/tenant, 30s connect, 15m idle, 60m max, 1 MiB queue cap.
   - Wire frame schema assigned exclusively to `hub/pkg/terminal`.

## Verification Commands & Output

### 1. Verification of Specification Document Structure
```console
$ test -f docs/REVERSE_PROXY_AND_TERMINAL_SPEC.md && echo "Specification exists"
Specification exists
```

### 2. Verification of Contract Consistency Across Codebase
```console
$ grep -E "OMINULL-ENDPOINT-GRANT-V2" agent/include/response_dispatcher.h hub/pkg/responseauth/proof.go
agent/include/response_dispatcher.h:#define OMINULL_GRANT_DOMAIN_TAG "OMINULL-ENDPOINT-GRANT-V2"
hub/pkg/responseauth/proof.go:	grantV2DomainTag = "OMINULL-ENDPOINT-GRANT-V2"
```
